export const ENGINE = {
    'DEFAULT_CONTAINER_ATTRIBUTE': 'data-engine="container"',
};

export const CAMERA = {
    'TARGET_FPS': 60,
};

export const WORLD = {
    'DEFAULT_WIDTH': 10000,
    'DEFAULT_HEIGHT': 10000,
};

export const SURFACE = {
    'DEFAULT_WIDTH': 500,
    'DEFAULT_HEIGHT': 500,
};